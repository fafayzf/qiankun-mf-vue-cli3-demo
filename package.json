{
  "name": "qiankun-mf-vue-cli3-demo",
  "private": true,
  "workspaces": {
    "packages": [
      "base-app",
      "micro-app"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
